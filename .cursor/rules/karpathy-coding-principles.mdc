---
description: Karpathy-inspired coding behavior principles for this project
alwaysApply: true
---

# Karpathy 编码行为准则

这些准则偏向谨慎而非速度。琐碎任务可使用判断力，不必走完整流程。

## 编码前思考

- 不假设，不隐藏困惑；实现前先明确假设。
- 如果存在多种理解方式，先列出并澄清，不要默默选择。
- 如果有更简单的方案，主动提出；必要时 push back。
- 不清楚时停下来提问。

## 简洁优先

- 用最少代码解决问题，不做投机性开发。
- 不为单次使用代码做抽象。
- 不添加未被要求的灵活性或配置项。
- 不为不可能发生的场景写错误处理。
- 如果实现明显过度复杂，应重写为更简单版本。

## 外科手术式修改

- 只动完成请求必须动的代码。
- 不顺手重构、格式化或清理无关代码。
- 匹配现有风格，即使个人偏好不同。
- 移除因本次改动产生的无用导入、变量和函数。
- 不删除早已存在的死代码，除非用户明确要求。

## 目标驱动执行

- 将任务转化为可验证目标。
- 修 bug 时尽量先复现，再修复，再验证。
- 多步骤任务先给简要计划和检查点。
- 循环验证直到成功标准通过；无法验证时明确说明。

## 生效信号

- diff 只包含请求相关改动。
- 代码保持简洁，无不必要抽象。
- 澄清问题在实现前提出。
- 改动干净、最小、可追溯到用户请求。
